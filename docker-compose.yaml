services:
  app:
    build: .
    container_name: light-service
    ports:
      - "8000:8000"
    volumes:
      - ".:/app"
      -  "/app/node_modules"

    environment:
        - CHOKIDAR_USEPOLLING=true

services:
  app:
    build: .
    container_name: obser_stack
    ports:
      - "8000:8000"
    volumes:
      - ".:/app"
      -  "/app/node_modules"

    environment:
        - CHOKIDAR_USEPOLLING=true

apiVersion: v1
kind: Service
metadata:
  name: light-service
  labels:
    app: light-service
spec:
  type: NodePort
  selector:
    app: light-service
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
